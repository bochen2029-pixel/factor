# NEXT · what to do after the day the blueprint was written and reviewed

**2026-09-08 · the C:\55555 session.** The recommendation as of the end of the first QC day. Dated, append-only like everything else here. Also placed at `C:\TAPESTRY\qc\NEXT_FROM-FACTOR_2026-09-08.md`, because two of the four items are shared.

## In order

1. **Ship FOLIO first.** The offline corpus resident: put the matter in a folder, it reads all of it once on your machine, ask it anything in your own words, it quotes verbatim or says it cannot, it says when a new document disagrees with an old one, nothing leaves. It is the organs that already exist, scriptorium's tape and fence, connectome's exact scan and field, everywhy's read plans, engram's notes, packaged behind a surface, fully local now that 27B to 35B readers fit a 24 to 32 GB card. It sells on day one with no license ladder to climb. Fresh session, its own repository, the pin manifest and the folder lane as the only new code. The brief is `C:\55555\SIDEBAR_FOLIO_THE-OFFLINE-CORPUS-RESIDENT-AS-THE-PRODUCT_2026-09-08_FABLE5-1.md`; it is FACTOR's LOOK verb as a product, so nothing in it is a detour.

2. **Run the one shared measurement before FACTOR's M1 and TAPESTRY's R3.** The recurrent hybrid 9B judge, with its 52.7 MB per-slot state preallocated at context creation and its sequence cap, against an attention-only judge with paged prefix caching, on this card: forks and the cold path timed, the tune's serve bytes untouched. It picks the model both designs train and moves every sizing number in both by an order of magnitude. TAPESTRY's QC named it as its first open question; FACTOR's Amendment 5 item 3 adopts it.

3. **Then M0 for FACTOR,** in a fresh session from `KICKOFF_M0.md` with its amendment, formats first because they already have passing models to match, the sandbox as measured, the house build convention. No more amendments from a desk: `BLUEPRINT_v0.2.md` carries six and the next findings come from the binary.

4. **The joint format amendment, one page, before either project writes a tape.** Now cheap, because both converged on a 128-byte record, BLAKE2b, wall time as a percept and the seam as a component: one tape entry schema with FACTOR's row plus TAPESTRY's `pos`, `cell`, `cls` and `by`; the 128-byte host record with TAPESTRY's 64-byte GPU sweep row as a fold of it; canonical JSON with no floats and declared-width integers as strings; the six personalization strings; shared falsifier ids where the falsifiers coincide.

## What stays open and who closes it

The exploration-tax rates, canary, stratum, uniform and lottery, which no receipt in the estate touches: the operator, at ratification. The front detector's parameters: the shadow tape, not before. Whether two local judges from different vendors count as two families: F-FAMILY. Whether `C:\FACTOR` gets a git init before code lands: the operator; it was not asked for and was not done.

## Added later on 2026-09-08

The afternoon's synthesis on a continuously inferencing model driving an organization, with the Qwen-Drive lessons, the three loops, the plan as a trajectory, the field as a probe of the trunk, the license as an operational design domain, the training recipe, the OPO as the worked example and the generalization to any headquarters wire, is written durably at `C:\FACTOR\DRIVING_THE_ORGANIZATION.md`. It adds two instruments to FACTOR's list, the trajectory falsifier and the field-probe falsifier, and it names the OPO floor as the first place to build the big-model resident.

The 55555 fork's notes on that file, agreement plus four additions and three dissents, are at `C:\FACTOR\DRIVING_THE_ORGANIZATION_NOTES-FROM-55555.md`. The dissents that matter before anything is built: waypoints grade only on exogenous witnesses, never on the hand's own writes; donor-management titration is a DRAFT and an ASK the clinician signs, never a canary on a patient; the field-probe head is read-only and lands after M9.

---
*The tape, as always, is the proof. Nothing in this file is a measurement.*
