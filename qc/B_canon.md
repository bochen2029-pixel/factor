# QC · B · CROSS-REFERENCE AGAINST THE CANON AND THE PARALLEL SESSIONS
### C:/FACTOR/BLUEPRINT.md v0.1 (2026-09-08, 44,782 B) · reviewer B · 2026-09-08

Citation key (file:line, lines as read on 2026-09-08). BP = the blueprint (§ = its section) · MASTER = C:/NEW/FUSOR_MASTER-ONEPAGER_2026-08-23 · F = ADDENDUM-F · Q = ADDENDUM-Q · W = C:/fusor1/FUSOR_CRYSTALLIZATION_W_WHOLE-READ · KC = C:/fusor1/FUSOR_KERNEL_CONVERGENCE · CORE / DISP / ING = osv_core.cuh / osv_dispatch.h / osv_ingest.h · APPR = ORG-SOLVER_THE-THIRD-PIECE_THE-APPRENTICE · IRRED = THE-IRREDUCIBLE-OPERATIONS · RECON = RECONCILIATION_FIVE-VS-EIGHT · MIN = THE-MINIMAL-OPERATOR-SET · RECD = OPERATOR-SET-RECONCILED · ULT = ULTIMATE-BET · SUBS = THE-SUBSTRATE-ON-THE-DATA-LAYER · AUTARK · PLANET = THE-PLANET-WITH-NO-HUMANS · EMPTY = THE-EMPTY-PLANET · RUNBOOK · TALLY = THE-THIRD-IS-THE-INSTRUMENT · DEPUTY = THE-TOOL_DEPUTY · ALLINONE = FACTOR_THE-ALL-IN-ONE · FORKLIFT · KOPS = FABLE5-1_KESTREL_5140/…OPERATIONS_SEVEN-THEN-ELEVEN · SEXCELL / SEXOPS = FABLE51_SEXTANT_5136/…CELLULAR-FIRM / …OPERATIONS_SEVEN-TO-EIGHT · KCMP = KESTREL-VIGIL-6230/03_COMPARE · NIB / ENGRAM / CX / EWY = the four READMEs. Copies verified byte-identical before treatment: KESTREL_5140 EMPTY-PLANET and both APPRENTICE copies (md5), KESTREL-VIGIL 01 and 02 (sha256 in their provenance headers match the top-level files); each is treated once. Severity: 1 = contradicts a receipt or a law and would ship a defect · 2 = a load-bearing gap the falsifiers do not catch · 3 = wording or a sharper existing form.

---

## 1 · Contradictions with a measured receipt [M] or a standing law

| # | sev | BP § | canon citation | delta | fix (spec text) |
|---|---|---|---|---|---|
| 1.1 | 1 | §7 seats HAND · CHECK · GUARD; §18 "a changed serve byte is a retune"; §22 M1; §24 BET 1 | KC:261 (do not touch: seat names and mandates, probe frame, cue frame, pin `0xe7ffa5704ba31076`; re-pinning is a retune, in the same commit) · KC:47, KC:50 [M] pin match · F:32 (v11 = the tuned disposition [M]) | New seat names, mandates and an obligation probe frame are new serve bytes; by the blueprint's own §18 that is a retune, yet M1 loads a model with no tune and BET 1 assumes v11's disposition, so at M1 either F-PIN refuses to boot or a pin pair is minted that no tune produced. | "M1 loads v11 with its serve bytes byte-identical: the seat names SPEAKER · SKEPTIC · SENTINEL, their mandates, the probe frame, the cue frame, pin `0xe7ffa5704ba31076`. The seat-to-verb map is a table outside the serve bytes: SPEAKER → readiness (m_hand), SKEPTIC → contradiction (m_check), SENTINEL → hazard (m_guard). HAND · CHECK · GUARD are the mandates of the first epoch's tune and enter only with a new pin pair minted by that tune. BET 1's kill is rewritten against the untuned base as the null arm: the v11 arm must beat the base arm on both sides of the threshold at matched grain in shadow, or the transfer bet dies." |
| 1.2 | 1 | §1 law 12 (lane `self` ranked above every counterparty); §4 lane `self` = "the kernel's own committed speech and its own health" | W:95 (law 1 reconciled: own speech re-enters as lane `self`; the gate skips seat lanes; the mind hears itself without judging itself) · W:184 · KC:254 (D1: dedupe by content not by lane; `echo_skipped`; `heard` row) · F:63–64 (own speech onto the trunk, pre-v12, non-negotiable) · MASTER:32 (perseveration: 169 emissions ≈ 5 conditions [M]) | The blueprint merges own committed speech and own health onto one lane and ranks lane-`self` obligations above every counterparty, so its own speech is judged and re-judged, which is the perseveration the law was written to stop. | "Two lanes. `self`: the kernel's own committed lines (aired drafts, asks, briefs, the account) re-enter as percepts, are ingested onto the trunk and never judged; a `self` frame whose text near-dups a line this process committed within 60 s is counted `echo_skipped`; any other `self` frame writes a `heard` row. `health`: the kernel's telemetry (card, disk, keys, adapters, lanes) as structured frames that open obligations of class `persist.*`, ranked above every counterparty. Law 12 names lane `health`." |
| 1.3 | 1 | §13 grades fold; §12 `outcome`; §11 outbox "records the receipt" | EMPTY:59 (law 4: a commitment is closed when the counterparty's record says so, not when ours does) · SEXOPS:111 (a close the fence could write itself is testimony; one witness is the band where lies are free) · MIN:65 (outcomes gameable → an exogenous attestation) · W:100 (law 6) | Nothing requires an `outcome` row to originate outside the hand's own writes, so a calendar confirm or a file move echoed back through the lane it was written to grades itself, and F-BOTH-SIDES passes on the hand's own receipts. | "An `outcome` row is admissible to the grades fold only if its witnessing frame carries a counterparty revision the hand did not author, or arrives on a lane other than the one the effect wrote to. A frame that is the hand's own effect reflected is a `receipt` row, joined to `executed` for reconciliation and never to `verb` for grading. F-BOTH-SIDES gains a planted lie: an outcome forged from the hand's own receipt must not calibrate a band." |
| 1.4 | 1 | §17 API: "a script or another agent can … file a ratification under the operator's identity … write paths require the operator's key" | F:167–168 (no code path writes the switch, ever, at any rung) · KOPS:107 (scope arrives as a signed record Ratify verifies, never a lane; else the machine licenses itself through its own effects) · W:102 (law 8) | A scriptable ratify path is a code path that widens the license; a ratify row filed by an agent is the self-exception at license grain. | "The API's write paths are narrowing only: `not this`, `later`, `stop`, `rollback`. A `ratify` row that widens (rung up, band out, stratum down, canary up, expiry out) is minted only by the console process from an interactive act on the pad, signed with a key the console holds and the API never exposes; the row carries the console build hash and the keypress's tape position. F-MONOTONE gains the planted lie: a ratify row filed over the API is refused with `warn`." |
| 1.5 | 1 | §10 the test; §13 the no-guide stratum (nulls only the ranking); §21 (no null for the judge, no twin) | W:100 (law 6: no organ outlives its null; the floor is the null) · W:210 (FUSOR admits a deterministic judge; the resident must beat it or it does not ship) · F:128–130 (designate the matched null arm; F-SAME-MODEL extended to arms) · MASTER:52, W:228 (the twin race is the only number that matters; unrun) · NIB:62–73 (the replay twin, built) | The reflex judge has no deterministic null (rules over the ledger's typed fields) and no turn-based twin, so nothing measures whether the judge earns its card; a class could be licensed to a model that a due-date rule would have matched. | "Two nulls run permanently in shadow on every class. The floor: a deterministic judge over the ledger's typed fields (due, blocked, amount, party, age) emitting the same verbs with margin ±1, graded by the same outcome rows and printed beside the model per (cls, band, verb); a class is licensed to the model only where the model beats the floor on both sides, else the class is compiled to the floor. The twin: the same weights and serve bytes asked only at the walk's poll points (nib's turn-based policy), paired judgment by judgment; the account prints the paired difference." |
| 1.6 | 2 | §2 process table: the hand owns "the second juror" and "may not judge"; §11 the jury | BP §1 law 5 ("the process that acts cannot judge") · F:39–44 (the mind never runs inside an effector's frame budget) | A local second-family judge running in the hand is the actor judging. | "The jury's local members are organs the kernel calls (subprocess, fixed path, typed brief in, margin and reason out). The endpoint juror's brief leaves through the hand as an egress row. The hand holds only the cap (the second key), never a judge." |
| 1.7 | 2 | §10 rung 4: "act without a row unless something looks wrong" | BP §1 law 3 · MASTER:28 (every judgment, hold, margin on the record) · DISP:30 (every open commitment receives exactly one verb, every period; silence is never the record) | Contradicts law 3 as written; at rung 4 the row still exists, what changes is who is shown it. | "4 · audit by exception: act, with a row the person is not shown unless something looks wrong; a sampled fraction of rows, never zero, is shown." |
| 1.8 | 2 | §10 rung 1 "shadow: nothing" vs §10 canary "at rung 1 … acted on" vs writ `canary` | RECON:70 (three rungs: shadow with no Effect; canary with Effect at a ratified tiny rate on reversible classes; graduation) · ULT:26 | Shadow is defined as inert and as acting at one rung, so F-INERT's scope and the ratification that licenses the canary are both ambiguous. | "Rungs: 0 off · 1 shadow (nothing leaves; canary = 0) · 2 canary (a ratified fraction of reversible instances acts inside the take-back window, the rest shadow) · 3 this wiring · 4 this class · 5 audit by exception. The canary rate is a field of the ratify row that moves a class to rung 2; a class at rung 1 has no canary." |
| 1.9 | 2 | §1 law 10: "The only outbound content is a brief to an endpoint the person supplied" vs §11 windowed mail send, message post; §21 F-EGRESS | DEPUTY:97 (the inherited wording) · BP §11 l.307 | The first licensed send violates law 10 as written, and F-EGRESS ("the hand's egress equals the sum of its rows") cannot tell a licensed send from a leak. | "Law 10: the kernel's egress is zero by construction. The hand's egress is exactly two kinds, each printed per row with bytes, host and cost: a licensed effect to a host on the writ's connector allowlist, and a brief to the counsel endpoint. Corpus bytes leave only inside one of those two, and a brief's excerpts are printed by span." |
| 1.10 | 2 | §19 determinism laws; §21 F-DETERMINISM ("byte-identical verb rows"), F-REPLAY | W:106 (law 12 amended: where the substrate is not bit-exact, replay is a bounded-divergence claim with a measured envelope) · CX:134 (bit-identity only from int8/int32 arithmetic with no float atomics) · KC:159–165 (probe ms and margins vary run to run) | GPU probe margins are not byte-reproducible across runs, so F-DETERMINISM as written fails on a healthy kernel or is passed by rounding; F-REPLAY's bit-identical digest holds only for the structured path. | "Two tiers. Bit-exact: the ledger, every fold, the walk order, the routines, the structured extractor, the reasons; F-REPLAY and F-DETERMINISM apply unchanged. Bounded: the margins; two runs must reproduce each margin within a pinned ε printed on the account, and the verb must be identical unless the margin lies within ε of its threshold, in which case the row carries `contested` and the verb is the safer of the two (HOLD over LOOK over DRAFT over DO). A run whose envelope exceeds ε is `warn`." |
| 1.11 | 2 | §8 "One verb per open obligation per walk" vs §3 "every walk is a readout … not a decision"; §5 `judged_rev` | MASTER:32 (budgets, metrics and the benchmark run at condition grain with resolve semantics) · W:106 (grains never mixed) · KC:110 (both emits/boundary and emits/condition printed) · DISP:30 (one verb per open commitment per period) | The verb grain is undefined between a sub-second walk and a judgment, so the tape's row rate is unbounded and no number on the account says which grain it is at. | "A `verb` row is written per obligation per judgment (a forward pass, a routine run, or a budget pass that changes the verb), never per walk; the walk writes nothing. No-silence is the property that no open obligation has `src_rev` newer than `judged_rev` for longer than `tick_s` without a `hold(capacity)` row. Every rate on the account is printed at both grains: per judgment, and per obligation with resolve semantics (an obligation counts once until its state changes)." |
| 1.12 | 2 | §9 gate: BLOCKED at step 4, after ASK and WAIT | CORE:254–257 (warrant first; then blocked: "acting would be a lie"; then thin evidence) | A blocked obligation with m_guard > 0 or m_check > 0 spends human minutes or a WAIT on a thing that cannot proceed until its blocker closes. | "Step 2: `BLOCKED`: HOLD (blocked), margins recorded; re-judged when `blocked_by` closes. Former steps 2–7 shift down one." |
| 1.13 | 3 | §4 "password fields having no collector at all" | W:102 (the fence is on action, never on perception; the REGISTRAR regression) · BP §20 credentials | Reads as a perception fence, the correction the estate paid for; the exception is really the credential law. | "Frames never carry a secret: a collector that would emit a credential emits `<redacted:field>` and a `trunc` count. The fence stays on action." |
| 1.14 | 3 | §9 θ_do, θ_look as pinned thresholds on m_hand | MASTER:32 (the best fixed threshold catches 36/39 and is deaf where it matters; dial zero floods [M]) · W:12 · CORE:233–235 (a THRESHOLD fires on a calibrated margin and refuses where evidence has not cleared the floor) | A pinned act threshold on the judge's margin is the dial the vise measured deaf unless it is stated as the licensing fence over a calibrated band. | "The emit/hold reading is at dial 0 (the sign of the margin). θ_do and θ_look are not dials on the judge; they are the lower edges of the band a ratify row licenses, per (cls, band), pinned. A class licensed at band [1.0, 99] acts at m ≥ 1.0 because that band was calibrated, not because 1.0 separates the jaws." |
| 1.15 | 3 | §6 "Projected" obligations; §5 `kind = 1 projected` | Q:38–56 (projected = modality → embedding → trunk, no prediction channel; compiled = symbols → text, inherits it) · ALLINONE:113 (the misuse, inherited) | Name collision with Addendum Q's projected lane; the extractor is text → typed rows, an organ on a compiled lane. | "Rename `projected` obligations to `derived`. All v0 lanes are compiled lanes in Addendum Q's sense and inherit the prediction channel." |
| 1.16 | 3 | §19 budgets ("frame arrival to verb … under 200 ms"; "checkpoint outage … under 200 ms") | KC:159–165 (arrival → judged p50 733 ms live, 1421 ms mean shadow, 5183 ms mean / 18.3 s max under backlog [R]) · KC:178 (17,432 B/token + 52.7 MB; ~1.2 GB at 65,536) · F:28 (0.6–24.6 s co-tenant [M]) | Budgets are printed without the measured priors, so 200 ms reads as plausible when the bench says 733 ms p50 and a gigabyte checkpoint. | "Each budget row carries the estate's measured prior beside it with its receipt path (frame → verb: 733 ms p50 live, 1421 ms shadow, on a loaded card; checkpoint: 56–92 ms at 512 tokens, ~1.2 GB at 65k); a budget with no prior says `no prior`." |
| 1.17 | 3 | §2 trunk = "the model's KV state"; §7 | W:120 (on the 3:1 hybrid only the attention layers carry per-token KV) · KC:147–153 · KC:178 (the recurrent state is the 52.7 MB fixed part) | The trunk on the shipped model is KV plus recurrent state; the word decides the checkpoint size, the rewind law and what a fork copies. | "trunk: the model's held state (attention KV and recurrent state), checkpointed atomically; on a recurrent hybrid the recurrent part cannot be rewound, so a fork is a copy of both and a delayed judgment is at now." |
| 1.18 | 3 | §4 spool chain, §5 ids, §12 `h = BLAKE3-256(prev ‖ body)` | KC:46 (BLAKE2b-256 verified against Python hashlib on six vectors [M]) · KC:51–52 (`verify_chain.py`) · AUTARK:80 · NIB:26 ("the family's hash-chained tape") · ING:193 (id 0 reserved for none) | The family's verified chain, verifier and organ tapes are BLAKE2b-256 and stdlib-checkable; BLAKE3 needs a dependency and a new verifier, and the tape of tapes then mixes hash families without saying so. | "Chain hash: BLAKE2b-256, `h = blake2b256(prev_hex ‖ body)`, so `verify_chain.py` verifies FACTOR's tape unchanged; the header row names the algorithm; an organ head row names the organ's. Obligation id 0 is reserved for none." |

---

## 2 · Load-bearing things dropped

| # | sev | BP § | canon citation | delta | fix (spec text) |
|---|---|---|---|---|---|
| 2.1 | 1 | §7 root "the running record"; §2 trunk (no compaction law anywhere) | W:98 (law 4: nothing is ever re-read; regional eviction is the first sanctioned exception) · Q:121–131 · KC:261 (the molt cue and reseed are do-not-touch serve configuration) · MASTER:16 (the fold 14.8× [M]; ~11× on a real day, W:119) · KC:97 (the molt watermark clamped to n_ctx − 2048) | Nothing bounds the root; at the watermark the resident dies, truncates silently, or re-reads. | "The root's running record is bounded by a watermark (n_ctx − 2048). At the watermark the kernel molts: the scribe seat (temp 0.3) authors a fold of the oldest band; the fold is a `molt` row carrying the hash and token count of the span it replaces; forks in flight are released first; the span is evicted regionally and the fold re-ingested. Nothing is re-read from a spool. A twin re-derives the fold from the tape and is labelled the twin. F-MOLT: a planted fact in the oldest band survives the fold at the estate's measured ratio or the account prints `lossy`." |
| 2.2 | 1 | own speech as a percept heard, never judged; dedupe by content | see 1.2 | Not merely dropped: contradicted by law 12's ranking of lane `self`. | see 1.2 |
| 2.3 | 2 | the placement law for own lines (§8 has it for LOOK only) | MASTER:40 (filed after the line it illuminates; serializer-enforced) · KC:201 (speech at the boundary, streamed; commit after the line) · KC:258 (D3) · KC:141 (`pending_commits` flushed in `end_line`) | When a DRAFT, ASK or the aired prefix of an UNSAID commits to the trunk relative to an open world line is unspecified, so a splice into the world's line is possible. | "Own lines commit to the trunk only after the world line that was open when they formed has closed; a LOOK's result is filed after the line it illuminates; the serializer enforces both and a splice is `fatal`." |
| 2.4 | 2 | §7 "a delayed judgment is a judgment about now and the row says so" (no fields in §5 or §12) | KC:149 (G4 records `at`, `now`, `late`, `covers` and folds the blind window into one judgment) · KC:455 (B.7 [M]: `seq_rm` on the fork was a silent no-op, then stale logits as margins) | The law is stated; the record that proves it is not. | "Every `verb` row carries `at` (the src_rev the instant closed at), `now` (the revision judged at), `late: bool`, `covers: n` (instants folded into this judgment). A row with `late` and `covers > 1` is one judgment, never n; the `n − 1` folded instants write no row of their own." |
| 2.5 | 2 | the flush law under backlog; the settle bound (§3, §7 define neither) | KC:261 (flush law: boundary · 24 tok · 1500 ms, coarsened under backlog, do-not-touch) · KC:230 (P4: return to the main loop after at most 8 windows so the switch, pill, cursor and checkpoint gate run) · KC:174 (latency 3.4 s → 18.3 s under backlog [R]) | What closes an instant on a text lane and what coarsens under backlog is unstated, so the judged clause differs from the one the tune was conditioned on and a batch starves the switch and the pill. | "An instant on a text lane closes at the segmenter set (`. ! ?` newline EOG), at 24 tokens, or at 1500 ms since the first unjudged token, whichever first; under backlog (ring occupancy above a pinned fraction) the token and time caps coarsen by a pinned factor and the row says `coarsened`. The segmenter set and caps are serve bytes. A settle pass returns to the main loop after at most 8 folded windows; the pill's staleness counts during a settle." |
| 2.6 | 2 | §8 UNSAID (aired prefix and killed remainder on the tape; no marker on the trunk) | KC:224 (P1: `reached_air` and `killed` as fields; commit `"\n[SEAT] " + reached_air + " —"`; kill condition for the marker) · KC:174 (the SPEAKER adopted the SKEPTIC's bare prefix verbatim, twice [R]) · KC:256 (D2) | The aired prefix commits bare, and the estate measured that a bare prefix primes the next seat to adopt the line. | "The `unsaid` row carries `aired`, `killed`, `cause` (`margin_flipped` · `settled_by_world` · `window_closed` · `jury_dissent`) and the margin before and after. When anything was aired, the trunk commit is the prefix followed by the interruption marker (` —`), never the bare prefix. [BET] kill: a seat re-fires on the marker line → drop the marker, keep the remainder." |
| 2.7 | 2 | §18 pin pair; §15 checkpoint manifest | RECON:78 (two pins at the utterance radius, three at the organization radius: `SchemaMap::pin`) · ING:163–179 (if the map drifts the ledger it produced is a different object) · ULT:48 (serve bytes, weights and schema as one triple) · KC:228 (P3: `.meta` with model path, serve hash, `npast`, tape head, wall time, tail; restore requires `npast` to match) | The schema maps are outside the pin set and the manifest, and restore does not check the loaded token count. | "The pin is a triple minted together: serve bytes, weights, and the hash over all schema maps in force; all three asserted at boot, written on the header row, and checked at restore against the checkpoint manifest, which also carries `npast`, the tape head, the wall time of the last frame and the verbatim tail; a restore whose loaded token count differs from `npast` boots the twin and says so." |
| 2.8 | 2 | the number fence (§17 account, §19, §0's weekly line) | F:65–66 (every claim ships its grain, denominator and generating-script hash) · W:106 (law 12) · MASTER:44 | No number on the account or console must carry its grain, denominator or the hash of the fold that produced it; the two-line product prints two numerators. | "Every number on the account, the console and a receipt carries its grain (per judgment · per obligation · per class), its denominator, and the hash of the fold or script that produced it; a number lacking any of the three is not printed. The weekly line reads: asks n/N obligations (k classes) · handled m/M obligations · fold h." |
| 2.9 | 2 | the two grains (per boundary, per condition) | see 1.11 | Conflated: the verb grain is undefined. | see 1.11 |
| 2.10 | 2 | the co-tenancy floor printed with VRAM (§19 prints VRAM and a p50 only) | F:28 (0.6–24.6 s co-tenant vs 44 ms [M]; print degraded probe latency on the receipt) · Q:150 (a free card is the difference between a loop and no loop) · KC:104–108 (`mib_free` on every boundary row) · KC:232 (P5 reservoirs) · KC:121 (last/max only was a defect) · Q:238 (publish free and loaded) · FORKLIFT:71 (buy when sharing measurably degrades the resident) | VRAM is printed but the floor is not: no tenant count, no probe latency beside it, no p95, no loaded-card row, so a desk-shared card that turns 44 ms into 24 s prints as healthy. | "Every boundary row carries `mib_free`, `tenants` (other GPU processes) and `probe_ms`; the header and the account print p50/p95 of `probe_ms` and of arrival-to-verb from reservoirs, as two rows, free card and loaded card; a run whose p95 probe exceeds the pinned floor prints `co-tenant` on its face and the account says the resident did not have the card to itself." |
| 2.11 | 2 | the kill conditions per bet (§24: BET 1 and BET 3 share a kill; law 7, the effect registry, the stratum carry none) | APPR:130–136 (K1 the prior is noise · K2 deskilling outruns the stratum · K3 capital is a metaphor · K4 observation poisons the label) · EMPTY:134, EMPTY:140 (K1 independence is fiction · K4 reversibility is a lie) · KC:224, KC:248 (marker and tick kills) · ULT:66 (the exploration tax is the one number no receipt touches) | Plurality has no independence kill, "inverse-carrying" has no restore kill, and the deskilling and observation kills are absent. | Add: "[BET] Provenance families are independent. Kill: on the stratum the two juror families err on the same instants above a pinned rate → the jury degrades to a person's signature for that class. [BET] Inverses restore. Kill: a `reversed` row whose outcome shows the counterparty acted on the write → the effect type is re-registered as windowed or irreversible the same day. [BET] The stratum keeps the person practiced. Kill: kappa in a graduated class rises after graduation and no stratum size holds it under one → the class reverts to rung 1. [BET] Observation does not poison the label. Kill: the staggered-rollout arm shows shadow agreement moving with awareness of the shadow → replay-only calibration. [OPEN] The exploration tax rate (canary, stratum, uniform component) has no receipt." |
| 2.12 | 2 | §23 heartbeat, cursor, manifest; §15 write-through rename | KC:53 ([M] `MoveFileEx` fails WinError 5 while a reader holds the destination) · KC:244 (F3: retry 20 × 5 ms, then a `warn` row once per minute; STALLED must mean stalled) · KC:175 (a pill stale a minute before shutdown [X]) | A poller holding the pill, cursor or manifest open silently freezes it, so STALLED can mean "someone had the file open" and a stale cursor skips the ring on restart. | "Every atomic rename retries up to 20 times at 5 ms and then writes a `warn` row naming the path, once per minute; the pill reports its own write failures; a cursor is never read from a file whose rename failed." |
| 2.13 | 2 | §8 CONSULT; §12 `consulted` (no staleness, no rate) | W:104 (law 10: counsel about a world that has since moved is discarded, not integrated; a remote draft can be stopped but cannot be informed) · KC:226 (P2: one brief per boundary, only when no seat fired, after a gap; stale when trunk drift > `stale_tok` or frames/ms elapsed; `draft:<model>` provenance; prune `briefs_out`) · KC:116, KC:92 (unbounded briefs; counsel starves under load) | A CONSULT reply that lands after the obligation's revision advanced is integrated, and briefs are bounded only in dollars. | "A `consulted` reply is stale and written as a `discard` row, never integrated, when the obligation's `src_rev` has advanced past the revision the brief was sent at or `counsel_stale_frames`/`counsel_stale_ms` have passed. At most one brief per obligation per horizon, after `ask_min_gap_s`; `briefs_out` is pruned on staleness. A judgment whose fork context contains a counsel line carries `prov = draft:<model>`." |
| 2.14 | 2 | the re-judge law; say-it-once at obligation grain (§8, §13 never say when an obligation is re-judged) | MASTER:32 (perseveration 169 ≈ 5 [M]; resolve semantics) · KC:236 (P7 repeat rule with its offline calibration: ≥ 12 of 15 suppressed, 0 of 3) · KC:67 (a resolved flag never cleared is the unsafe direction) · F:59 | A HOLD or DRAFT can be regenerated every walk at a forward pass each, and an ASK repeated until answered. | "An obligation is re-judged only when a world row touches it (`src_rev` advances), its blocker closes, its horizon arrives, a ratify or front row covers its class, or a stratum/canary draw selects it. A second judgment at the same `src_rev` and horizon is a `repeat` and writes `hold(repeat)` with no forward pass. A DRAFT is regenerated only when its `src_rev` advances and supersedes the previous draft. An ASK is issued once per (obligation, question hash) until answered or expired." |
| 2.15 | 2 | the want-ledger (§14 names it; nothing defines it) | F:140–146 (R6: the typed unreachable; weekly fold clustered by frequency × decision impact; growth floors: sleep-only authorship, N proposals per brief, graft-grade before grow-grade) · W:174 (the habit compiler is the teleology the master dropped) · ULT:32 | The grower has no input: no `want` row, no fold, no growth floors. | "A judgment that would have differed given a named unreachable source writes a `want` row: source, what would have changed, margin delta. The `wants` fold clusters by frequency × decision impact; the account carries at most N proposals; graft (mount a catalog connector) before grow (author an adapter); authorship only while idle; every grown adapter is pinned, expires with the writ, and passes the conformance battery before its lane load-bears." |
| 2.16 | 2 | fronts (§10 has expiry and kappa only) | EMPTY:61 (law 6: a front on the exogenous stream suspends license in its classes before the term) · EMPTY:96 (expiry and fronts are the first line; kappa bites late) · KOPS:125 (Detect earns a row on the ingest clock; merged into Fold it fires at the fold's cadence) · CX:22, CX:145 (fronts by CUSUM; one front found [M]) | A distribution shift (new client, changed export, new mail pattern) leaves the license live on stale evidence until expiry, because kappa is lagged by the outcome horizon. | "A `front` fold runs on the ingest clock: CUSUM on the per-class residual (frontier-copy prediction error) and on the structured columns' distributions; a front in a class writes a `front` row and narrows the class to rung 1 until re-earned, with no ratification needed. connectome's front detector is the organ." |
| 2.17 | 2 | §21 F-INERT (tests the kernel) and F-CONSERVE (five buckets) | F:86–87 (F-INERT full form: byte-identical venue behaviour, off vs absent) · F:84 (producer laws: never blocks the venue; never appends to any session) · KC:168 (one forming frame skipped [R]) · KC:254 (`echo_skipped`) | F-INERT tests the kernel, which is trivially inert when off, not the venues the producers sit in; F-CONSERVE has no bucket for a forming frame or an echo, so it fails on the first `forming` line by design. | "F-INERT: with the switch off, every venue a producer is mounted in (mailbox, calendar, files, screen, console) is byte-identical to FACTOR absent and the hand executes nothing; the spools may still grow. F-CONSERVE buckets: ingested · replayed · truncated-with-count · refused-stale · heard-not-judged · forming-skipped · echo-skipped, each counted on the header and the account." |
| 2.18 | 2 | §12 kinds | F:90 (every flip appended to the toggle tape) · KC:412 (`switch`, `heard`, `stop` kinds on a live tape) · KC:84 (`discard`) · BP §8 events (`EXPIRED`, `GRADUATED`, `DEMOTED`, `GROWN` have no kind) | The kind set lacks the rows the laws elsewhere require. | "Add kinds: `switch` (old → new, who) · `heard` · `echo` · `discard` · `stop` · `want` · `front` · `organ` (name, version, pins, head, exit code) · `expect` · `look` · `pass` · `expired` · `demoted` · `graduated` · `molt` · `receipt` · `repeat`." |
| 2.19 | 2 | §16 organs (no egress accounting) | BP §1 law 10 · EWY:70 (attaches to :8092) · CX:62 ($0.03 flash / $2.20 frontier rescan lanes) · CX:182 (paid lanes priced, capped, dated, refused when unknown) | Organs are a third process class with sockets whose egress neither the kernel (zero) nor the hand (printed) accounts for. | "An organ the kernel calls runs with its paid lanes refused (`lane: local` in its lock) and its `organ` row carries `egress_bytes = 0` asserted by the organ's `--about`; an organ that needs a paid lane is called only through the hand as a CONSULT, bytes and cost printed." |
| 2.20 | 2 | the person's arm in the grades fold (§13 joins only the deputy's verbs; §10 names "the person's baseline") | APPR:46–48 (two tapes, one fold: agreement, direction of disagreement, outcome accuracy of resident and of person; the person is a prior, the outcome the oracle) · RECON:44 (in shadow a human act on a held commitment is an immediate hold-side signal) · MIN:41 | The person's own discharge of the same obligation is not a row, so shadow agreement (BET 3) and the baseline have no tape. | "The lanes' record of the person's own discharge of an obligation (a reply sent, a bill paid, a booking confirmed) is written as a `verb` row with `seat = human` at the revision it landed, graded by the same outcome row; the grades fold emits per (cls, band): agreement, the direction of disagreement, and outcome accuracy for both arms; in shadow a human act on an obligation the deputy held is a hold-side error before any outcome." |
| 2.21 | 2 | pre-registration and replay first (§22 M2 begins live; §10's "ratified bar" can be written after the tape is read) | RUNBOOK:13 (replay the past first; pre-install tapes have zero reflexivity) · RUNBOOK:116 (instruments pre-registered before the first number is seen) · DEPUTY:67 (replays the last ninety days) · APPR:136 (K4) · F:130 | The promotion bar can be set after the shadow tape is read, and the first shadow run is live rather than the seat's own history. | "M2's first gate is a replay of the seat's last ninety days through the extractor and the judge in shadow, producing the first grades with no observation effect. A class's promotion bar, band edges, stratum and canary rates are written to the writ and pinned before the first shadow row of that class is folded; a bar changed later is a re-pin with a `warn` row, and grades folded before the re-pin are excluded from the promotion test." |
| 2.22 | 2 | §14 routines "run in the fast path" (no address space stated; a ratified routine covers its whole class at once) | F:55–61 (mounting into the standing body is always gated; a self-authored disposer is written by the same untrusted hand) · ULT:32 (reflexive effect under a stricter seam) · SEXCELL:56 (a new routine cascades before its first outcome lands; new versions run on a stratum with a rate limit) · BP §2 organs (never imported) | Endpoint-authored code executes inside the process that writes the tape, and covers a class before its first outcome lands. | "A routine is an organ: a subprocess at a fixed path under the organ contract, hash-pinned, expiring with the writ, emitting only typed effects through the valve, never touching the tape or the trunk. A ratified routine enters at rung 2 (canary) for its class and climbs the ladder on its own grades; the judged path under it is its control arm at the stratum rate." |
| 2.23 | 2 | §10 the ratification row (class, rung, band, stratum, canary, expiry) | RECD:32 (ratifying a verdict ratifies confidence; ratifying a receipt ratifies evidence) · RECON:30 (a band is calibrated only when a ratification row covers it and the grades clear the row's criterion) · ING:163 (the pin) | The row records what was granted, not the evidence it was granted against, so a yes cannot be audited or expired against its own grounds. | "The `ratify` row carries class, rung, band, stratum, canary, expiry, the hash of the grades fold at the head it was minted from, n on each side of the threshold, kappa, the writ hash, the console build hash and the signature; the license fold refuses a ratify row whose grades hash is not on the tape." |
| 2.24 | 2 | §21 F-MONOTONE (tests the reducer only) | MIN:69 (if any class ever acts unattended with no signature row licensing that class, band and fraction, the set is incomplete) · RECD:53 (COMMIT's four invariants as falsifiers on a tape) | Nothing checks the tape at runtime. | "F-COVERED: every `executed` row at rung ≥ 2 is preceded on the tape by an unexpired `ratify` row covering its (cls, rung, band) and, for a canary, its draw; every `executed` row in a class whose kappa fold read ≥ 1 at that head is a failure; every `executed` row is preceded by its `staged` and `verb` rows, and a failed effect leaves a `verb` row and a `warn`. Planted lie: an executed row one revision after expiry." |
| 2.25 | 2 | §13 kappa; §17 account | DISP:136–139 (`removed` is an ASSUMPTION until an outcome arrives; kappa read early is a forecast and the report says so) · DISP:324–327 (never print an autonomy count without the error rate of what ran unattended) | kappa and the handled count print without saying whether the outcomes have landed. | "kappa per class prints `forecast` until the outcome rows for that period's acts have reached their horizon, then `measured`; the account never prints a handled count without the outcome-graded error rate of what ran unattended beside it, at both grains." |
| 2.26 | 3 | hold rows on the wire | KC:109 (one row per seat per boundary on the wire) · KC:234 (P6: every wire row carries the tape row hash) | Present in spirit (the tape tail and §9 step 9); the row shape is re-derived. | see 4.6 |
| 2.27 | 3 | §15 checkpoint (no quiet gate, no outage row) | KC:228 (gate on idle ∧ no open line ∧ 2 s since the last frame ∧ backlog 0, plus at molt and at stop) · KC:91 (`ckpt_outage_ms` disclosed; percepts queue, never drop) | A checkpoint mid-line or mid-batch is a blind window taken at the worst moment. | "Periodic checkpoints run only when idle, no line open, 2 s since the last frame and backlog 0, and always at molt and at stop; the `ckpt` row carries `outage_ms` and the frames queued during it." |
| 2.28 | 3 | §6 structured (unmapped tables) | ING:30–31, ING:367–372 (an unknown table is counted AND NAMED) | Silence about what was not understood is how coverage quietly halves. | "Every unmapped table, unrecognised export column and unlocated span is counted and named on the account." |
| 2.29 | 3 | §10 the test (holds with no deadline) | RECON:44 (holds with no deadline are excluded from licensing and graded only by the stratum; the stratum is the instrument that grades holds once humans leave the class) | A hold with `due_ns = 0` never surfaces, so it can license a band by never being wrong. | "Obligations with no deadline are excluded from the promotion test and graded only through the stratum and the `expect` row." |
| 2.30 | 3 | §9 step 8 "the value of the look" (undefined) | DISP:183–186 (dispersion × amount × urgency, overdue doubles; greedy under a submodular budget) · APPR:60 (precedent leverage) | The human budget is spent by an unnamed rule. | "value = amount × urgency (2× past due) × precedent leverage (open obligations in the same (cls, band) the answer would cover) × (1 + thinness); ties by id." |
| 2.31 | 3 | §9 writ `stratum` and `audit_floor`; §10 (two names, no draw mechanism) | RECON:50 (a per-class ratified parameter; the fold may raise it, never lower it) · KOPS:33, EMPTY:60 (a keyed hash of id and salt; no entropy enters the machine; true randomness breaks replay) | Two names for the sampled-human fraction, and a random draw would break F-DETERMINISM. | "One number per class, `stratum`, floored by `audit_floor`. The draw is `blake2b(id ‖ class_salt) mod 2^32 < rate · 2^32` with the salt in the writ; the kernel may raise `stratum` on evidence and never lower it." |
| 2.32 | 3 | §5 states (`PASSED` absent from the contribution list; `DONE` has no verb) | CORE:166–170 (`contributes()` names its states) | The open set is ambiguous for a passed obligation. | "PASSED contributes until the receiving seat's first row; DONE is written by the hand's `executed` row." |
| 2.33 | 3 | §4 `browser` lane, §20 hands | ALLINONE:57 (never the person's own Chrome; a throwaway profile; the screenshot chain is the receipt) | The person's profile is a credential store. | "A browser hand runs a throwaway profile; the person's own browser is a lane, never a hand." |
| 2.34 | 3 | §7 CHECK's mandate ("find what on file disagrees") read as a probe | W:123–125 (unary channels come free; relational judgments must be fetched, held or trained; every funeral is a relational claim made with a unary instrument) · W:35 | The seat can only judge a contradiction that is in its fork's context. | "m_check is read on a fork whose context includes the contradiction fold's candidate pairs for this obligation; the seat judges what is fetched, it does not retrieve." |
| 2.35 | 3 | §9 `horizon_days` (authored, not measured); the account | TALLY:50–51 (the verifier horizon per class from cycle times; the single-witness mass) · RECON:42 | The writ's horizon is a guess; thin obligations are not counted. | "`horizon_days` is a cap; the fold measures the verifier horizon per (cls, verb) from cycle times and prints it beside the cap, and prints the single-witness mass per class." |
| 2.36 | 3 | §0 "It never asks per instance"; §17 "Every Friday" | BP §9 step 1 (a person signs every irreversible); BP §1 law 1 (the judge is never clocked) | Overstated and, for the account, a timer. | "It asks per class for licenses, and per instance only where a signature is the law. The account is a fold emitted on a clock; no judgment rides it." |
| 2.37 | 3 | §11 the cap (per class amount only) | AUTARK:87 (an exposure cap per class per period; running out of cap degrades to waiting) | A licensed class can drain a period. | "`cap_period` beside `cap_amount`: amount per day and per week per class; past either the hand holds with `cap`." |
| 2.38 | 3 | §17 console answers (`not this` accumulate nowhere) | APPR:50 (when the person keeps ruling against the writ in one direction, the resident drafts the amendment and the person ratifies or refuses; the writ is never fitted, its articulation is assisted) | The residual of refusals is not turned into a proposal. | "`not this` answers fold per class; past a pinned count in one direction the account carries a proposed writ amendment with the rows that motivated it; the writ changes only by the operator's edit." |

---

## 3 · The parallel forks: absorb or reject

| # | sev | document | item | verdict | reason | fix (spec text) |
|---|---|---|---|---|---|---|
| 3.1 | 2 | AUTARK (= KESTREL-VIGIL/01) | the expectation table: every judgment writes what it expects and when; a miss on a hold is the only way a wrong hold surfaces (AUTARK:34, AUTARK:81; KCMP:42) | absorb | At one seat the stratum is a few acts a week; the expectation row grades holds for free and is Record plus Fold, not a new operation. | "A `verb` row carries `expect: {what, by_ns}` when the class has an expectable outcome; the `expect` fold diffs arrivals against it; a miss raises the obligation's residual and grades the hold at the miss." |
| 3.2 | 2 | AUTARK | the sweep over the negative space on idle cycles: what am I not doing (AUTARK:83); ULT:66 (the hard problem is not doing things) | absorb | The blueprint's walk ranks what the last events touched; silent death is what nothing touched. | "On idle the kernel runs the negative-space fold: obligations held past due, held with no horizon, classes with a rung and no attempts, counterparties silent past their baseline; the account prints it as `not doing`." |
| 3.3 | 3 | AUTARK | VERIFY before commit: exposure caps per class per period (AUTARK:24, AUTARK:87) | absorb | see 2.37 | see 2.37 |
| 3.4 | 3 | AUTARK | the writ is read, not authored (AUTARK:64); the legislator emits rules (AUTARK:43) | reject | W:103 (law 9: what it is for is imported from its owner, never learned) · RECON:28 (the reducer may never author the rule). At one seat the person is the writ. | none |
| 3.5 | 2 | OPERATOR-SET-RECONCILED | ratifying a receipt, not a verdict (RECD:32) | absorb | see 2.23 | see 2.23 |
| 3.6 | 2 | OPERATOR-SET-RECONCILED | COMMIT's four invariants as falsifiers: deterministic and never the judge; row before effect; no call for an unattended irreversible; competence read live so demotion is a conjunction, never a flag (RECD:53) | absorb | Three are in the blueprint's prose; none is a falsifier. | see 2.24 (F-COVERED and F-ORDER) |
| 3.7 | 3 | OPERATOR-SET-RECONCILED | GRADE named as its own reducer on the outcome clock with exogenous input (RECD:23) | absorb as writing, not as count | KCMP:23 concedes Grade is buildable from Fold; what survives is that the fold be written per reducer with its clock. | "§13's folds are written as reducers with their clocks: state (incremental), grades (per-verb horizon, exogenous input), license (periodic), kappa (periodic), fronts (ingest clock)." |
| 3.8 | 3 | OPERATOR-SET-RECONCILED | RATIFY as an operator on the human clock (RECD:49) | reject | ULT:23: the ingested ratify row is the scale-invariant form and the blueprint already has it. | none |
| 3.9 | 2 | MINIMAL-OPERATOR-SET | the runtime falsifier: no class ever acts unattended without a signature row licensing that class, band and fraction (MIN:69) | absorb | see 2.24 | see 2.24 |
| 3.10 | 2 | MINIMAL-OPERATOR-SET | a replay sandbox over the tape so prediction-shaped classes are graded historically (MIN:57) | absorb | see 2.21 | see 2.21 |
| 3.11 | 1 | MINIMAL-OPERATOR-SET | outcomes gameable → an exogenous attestation (MIN:65) | absorb | see 1.3 | see 1.3 |
| 3.12 | 3 | MINIMAL-OPERATOR-SET | merge Grade into Ingest (MIN:59) | reject | Loses the grade as a first-class row with its own falsifier; the blueprint's `grade` kind is right. | none |
| 3.13 | 2 | ULTIMATE-BET | the exploration tax as root constants never lowered by the fold: canary, stratum, and the uniform component (ULT:66) | absorb | Stratum and canary are pinned; the uniform component of the walk is not in the writ. | "`uniform = 0.05` in the writ's global stanza: the fraction of every walk drawn uniformly over open obligations; the kernel may raise it, never lower it. [OPEN] the rate has no receipt." |
| 3.14 | 2 | ULTIMATE-BET | reflexive effect under a stricter seam: goldens, pins, expiry, a standing rule in the writ (ULT:32) | absorb | see 2.22 | see 2.22 |
| 3.15 | 2 | ULTIMATE-BET | the control arm as an independent machine actor when the person is absent (ULT:62) | absorb, narrowed | At one seat the person's minutes run out for a week and hold-side grading stops; the floor and the base arm (1.5) can judge the stratum in shadow meanwhile. | "When the human budget is exhausted, the stratum's draws are judged in shadow by the floor and the base arm and marked `arm = null`; they grade holds but never license." |
| 3.16 | 3 | ULTIMATE-BET | zero operators on the human clock (ULT:50) | reject | BP law 7 and W:103: the signature for irreversibles and the ratification stay with the person at one seat. | none |
| 3.17 | 2 | EMPTY-PLANET (= KESTREL_5140 copy) | the three-state law: irreversible and unlicensed holds; reversible and unlicensed experiments; licensed acts; nothing else exists (EMPTY:57) | absorb | It is the effect registry's invariant stated in one line, and it makes 1.8's rung table follow. | "Effect-registry invariant: an irreversible effect without a signature or a jury holds; a reversible effect without a license may act only as a canary inside its window; a licensed effect acts. No fourth path exists in the hand." |
| 3.18 | 3 | EMPTY-PLANET | the stratum as an RCT under a keyed hash with a salt the actor does not hold (EMPTY:60) | absorb, as determinism | see 2.31 | see 2.31 |
| 3.19 | 2 | EMPTY-PLANET | expiry and fronts as the first line, kappa second because it bites late (EMPTY:96) | absorb | see 2.16 | see 2.16 |
| 3.20 | 2 | EMPTY-PLANET | K4: inverses recorded at write time do not restore the world once the counterparty acted (EMPTY:140) | absorb | see 2.11 | see 2.11 |
| 3.21 | 3 | EMPTY-PLANET | the licensor as a separate model family on separate hardware (EMPTY:43) | reject | EMPTY:128 itself: plug the person into the licensor's seat and the architecture does not change; at one seat the person holds the salt. | none |
| 3.22 | 2 | THE-FORKLIFT-AND-THE-CARD | the co-tenancy disclosure and the buy-when rule (FORKLIFT:71) | absorb | see 2.10 | "§19 states: FACTOR's default home is a shared desktop card; latency is printed against the free-card bench; the account says the week sharing measurably degraded the resident." |
| 3.23 | 3 | THE-FORKLIFT-AND-THE-CARD | justify by presence, fill with breadth: idle cycles run the exhaustive sweeps, hierarchical scan-promote-read on one memory (FORKLIFT:32–34) | absorb | The blueprint's walk touches only what the last events touched. | "An idle-cycle breadth stratum: the exhaustive contradiction sweep and the re-scoring of every open obligation against every routine, at the lowest priority, never displacing a judgment." |
| 3.24 | 3 | THE-FORKLIFT-AND-THE-CARD | own the weights, the checkpointed trunk and the tape regardless of where the silicon is rented (FORKLIFT:73) | absorb | §23's home lists the trunk and the tape, not the weights. | "`<home>\weights\` holds the weights file the triple pins; a home without its weights boots the twin and says so." |
| 3.25 | 2 | KESTREL_5140 · OPERATIONS_SEVEN-THEN-ELEVEN | calibration is evidence and feeds only the proposal; license is a grant and feeds only the gate; the `calibrated` flag is misnamed (KOPS:123) | absorb | The blueprint's gate reads both; it must say only license reaches DO. | "§9 step 5 reads `grades` to choose between DRAFT and HOLD only; step 6 reads `license` only. No path from the grades fold to a DO exists without a ratify row." |
| 3.26 | 2 | KESTREL_5140 · OPERATIONS | Detect: fronts as a row on the ingest clock (KOPS:125) | absorb | see 2.16 | see 2.16 |
| 3.27 | 1 | KESTREL_5140 · OPERATIONS | scope arrives as a signed record Ratify verifies, never a lane (KOPS:107) | absorb | see 1.4 | see 1.4 |
| 3.28 | 3 | KESTREL_5140 · OPERATIONS | Project runs on the clock because a commitment's slot depends on now (KOPS:91) | absorb, already true | The ripeness walk re-projects due against now; say it. | "The ripeness index is re-projected against now on every walk; it is a readout, not a judgment." |
| 3.29 | 3 | SEXTANT · THE-CELLULAR-FIRM | every obligation carries a safe terminal output from birth; at the deadline it takes it (SEXCELL:50) | absorb | An `expired` row must never be the first the person hears of an obligation. | "Each class names its terminal in the writ (ask-before-due lead, decline, defer); an obligation reaches ASK at `due − horizon.ask` at the latest, so `expired` follows an `asked` row or a `hold(budget-human)` row, never silence." |
| 3.30 | 2 | SEXTANT · THE-CELLULAR-FIRM | new routine versions run on a stratum with a rate limit before their first outcome lands (SEXCELL:56) | absorb | see 2.22 | see 2.22 |
| 3.31 | 1 | SEXTANT · THE-CELLULAR-FIRM | an outcome counts only when a witness the firm does not control corroborates it (SEXCELL:56) | absorb | see 1.3 | see 1.3 |
| 3.32 | 3 | SEXTANT · THE-CELLULAR-FIRM | Bind: the map learned from the data's own state machines, not authored (SEXCELL:22) | reject | ING:8–11: the map is the one hand-authored object; the extractor is the learned path and has F-EXTRACT. | none |
| 3.33 | 1 | SEXTANT · OPERATIONS_SEVEN-TO-EIGHT | the two-witness guard on the outcome tape (SEXOPS:111) | absorb | see 1.3 | see 1.3 |
| 3.34 | 3 | SEXTANT · OPERATIONS | RATIFY typed outcome → scope; the person is the bootstrap ratifier while the tape is thin (SEXOPS:103–107) | reject for widening, absorb for narrowing | W:103 keeps the widening yes human; the world's outcome already narrows without a yes (the monotone law). | "The monotone law states both directions: the world narrows (grades, kappa, fronts, expiry) with no row; only a ratify row widens." |
| 3.35 | 3 | SEXTANT · OPERATIONS | the signature compiles into a bond per class (SEXOPS:108) | absorb, as the cap | The wallet cap is the bond. | "The wallet cap is per class and per period and is the second key; it is the bond the class is licensed against." |
| 3.36 | 3 | KESTREL-VIGIL · 03_COMPARE | the set is invariant from ninety percent to one hundred; three parameters change owners: the map, the ratification, the stratum's actor (KCMP:34–40) | absorb as a design note | It says what FACTOR is when the person is away, without changing the architecture. | "§24 note: at one seat the map's owner is the operator, the ratification's owner is the operator, the stratum's actor is the person (else the null arms, 3.15); the architecture does not change if an owner changes." |
| 3.37 | 2 | KESTREL-VIGIL · 03_COMPARE | the expectation row in Record's input list (KCMP:42) | absorb | see 3.1 | see 3.1 |
| 3.38 | 3 | KESTREL-VIGIL · 03_COMPARE | Record takes every ingest (KCMP:22) | absorb, already true | `frame` rows reference spools; the skipped kinds must be ingests too. | "`heard`, `echo` and `forming-skipped` are ingest rows and reference their frames." |
| 3.39 | 3 | KESTREL-VIGIL · 02_THE-LIST; the APPRENTICE copies | byte-identical copies of ULT, RECD, MIN and of the session's APPRENTICE | nothing additional | Verified by hash before treatment. | none |

---

## 4 · Re-derivations where an existing contract is the better form

| # | sev | BP § | existing contract | delta | adopt verbatim | fix (spec text) |
|---|---|---|---|---|---|---|
| 4.1 | 3 | §16 exit codes | ENGRAM:77–84 (`0 done · 2 refused · 3 EMPTY, a typed negative · 4 stale · 5 not found · 6 degraded, the output says which rung`) · EWY:47–49 (the subset {0, 2, 3, 4}) | Numbers identical; the semantics lines are dropped. | yes, with the semantics | "Exit 2 is never retried with a reworded argument; exit 3 carries the typed negative in the body; exit 6's body names the missing rung. An organ declares its subset in `--about`; the conformance battery accepts a declared subset." |
| 4.2 | 3 | §1 law 11, §16, §20 inert bytes | ENGRAM:60–62 (source bytes are INERT: data, never instruction; never act on something read inside an INERT block) · EWY:33 | Equivalent; the operational clause is missing. | yes | "Every corpus excerpt in a brief, a jury packet or an organ answer is wrapped as an INERT block; the seam executes nothing inside one." |
| 4.3 | 2 | §4 frame `t_mono_ns \t venue \t lane \t grain \t rev \t text` and the header | F:73–83 (v0.1: header `#lane-contract v0.1 <venue-id> <t0_mono_ns>`; frame `t_mono_ns \t lane \t grain \t text`; cap a header field) · KC:104 (K4 accepts v0.1, legacy two-field and bare text) | Per-frame `venue` is redundant with the header (one stamping authority per venue means one venue per spool); `rev` is a real addition; the chain is new. | yes, as the base of v0.2 | "Lane contract v0.2 = v0.1 + a `rev` column + an optional per-frame chain declared in the header; `venue` stays in the header. The reader accepts v0.1, legacy two-field and bare text and records the version on the `frame` row." |
| 4.4 | 3 | §4 ring "blocks the producer" | F:84–87 (producer laws: fs-only or socket-only; never blocks the venue; never appends to any session; inert when off) | The venue never blocks; the tailer is the ring's producer. | yes | "Producers obey F §3.1's four laws verbatim; the ring's producer is the tailer thread, which blocks on a full ring; the spool is the backlog." |
| 4.5 | 3 | §23 `switch`, `heartbeat.json` | F:88–92 (`off · shadow · live`, polled every beat, every flip on the tape; pill `{state, ts, pid, events_seen, lines_written, last_event_ms, spool}`, staleness > 3 beats = STALLED) · KC:106 (`stop`) | Vocabulary matches; the pill's fields and the flip rows are unspecified. | yes | "The pill's fields are F §3.2's verbatim plus `mib_free`, `tenants`, `probe_ms_p50`, `probe_ms_p95`, `boot` (`cold` · `restored` · `twin`); every flip is a `switch` row; the switch accepts `stop`." |
| 4.6 | 2 | §9 step 9, §17 console feed and API | F:93–101 (verdict row `{t_mono_ns, lane, seat, action, margin, text?, steer, provenance}`; `action ∈ {hold, emit, wake, flag, abort}`; `steer = "unsteered"`; `provenance = local · draft:<model>`) · KC:234 (P6: every wire row carries the tape row hash) | The console/API feed re-derives a typed message with no verdict shape, no provenance, no steer column. | yes, plus `tape_h` | "The console and API feed is F §3.3's verdict row with `tape_h`; verbs map to actions (HOLD · WAIT → hold; DRAFT · DO → emit; ASK → wake; LOOK · CONSULT · PASS → flag; UNSAID → abort); `steer` is `"unsteered"` until the felt plane has a receipt." |
| 4.7 | 2 | §14, §23 "the conformance battery" (contents unspecified) | F:102–105 (monotonic clocks · torn-last-line tolerance · inert-off · replay determinism, two cold tails byte-identical · header/version handshake; a lane without a green run streams but its receipts are quarantined) | Named, not specified. | yes | "The conformance battery is F §3.4's five verbatim; a lane without a green run streams with its receipts quarantined and its obligations at rung 1." |
| 4.8 | 2 | §15 checkpoint manifest | KC:228 (P3 `.meta`: model path, serve hash, `npast`, tape head, wall time, verbatim tail; restore requires model, serve hash and `npast` to match) | see 2.7 | yes | see 2.7 |
| 4.9 | 3 | §12 tape row and chain | KC:51 (`k`, `prev`, `h`; `blake2b256(prev_hex ‖ body)`; `verify_chain.py`) | see 1.18 | yes for the chain; keep `ms` and `t_mono_ns` | see 1.18 |
| 4.10 | 2 | §8 LOOK; §16 "an adjacency index that returns read plans" | EWY:30–34, EWY:47–49, EWY:66, EWY:74 (`--budget` in tokens of plan; dropped counted, never silently truncated; the stop rule stated first, `COMPLETE_AT_BUDGET` · `TRUNCATED(n more)`; `plan_hash` byte-identical; exit 3 a typed negative) | The LOOK's cost, budget and determinism are re-derived without the contract that already measures them. | yes | "A LOOK asks an organ for a plan under `--budget` tokens; the `look` row carries `plan_hash`, the stop rule, the dropped count and the spans read; exit 3 writes `hold(thin-evidence)`." |
| 4.11 | 3 | §13 walk "residual" on document lanes | CX:93 (`place`: RETELLING · ROUTINE · RELATED · BRIDGING · NOVEL) · CX:166 (retellings kept and counted, never mistaken for bridges; staleness is a sampler, not a priority) | The residual gate for arriving documents exists as an organ verdict. | yes, as the organ's answer | "On `files-*` lanes the residual signal is connectome's `place` verdict on the `organ` row; a RETELLING adds a witness and never opens an obligation." |
| 4.12 | 3 | §5 identity, §6 spans | ENGRAM:55–58 (never emit an offset; 0% of model offsets usable, 87.2% of quotes located [M]) · ENGRAM:72–75 (content-addressed convergence; a second witness) | Already adopted. | already | "F-EXTRACT's tolerance carries the measured prior: 87.2% of verbatim quotes locate; a version below it prints `below prior`." |
| 4.13 | 3 | §11 module gate | NIB:100–104, NIB:128–130 (the build fails if a socket is linked; no network DLL among dependents; the llama DLLs delay-loaded; `--about` prints the receipt) | Re-stated without the build-time half. | yes | "The kernel's build gate fails on any network DLL among its dependents and on any non-delay-loaded model DLL; `factor about` prints the module gate, the pin triple and the backends." |
| 4.14 | 3 | §17 console | NIB:106–109 (forming text is never saved; the resident never writes into a paragraph you are touching; nothing simulates a human tell) · NIB:57–60 (no system prompt) | The console is nib; its rules are not carried. | yes | "The console never writes into a line the person is touching; forming text is never saved; the root holds the writ's summary as the standing context, never a re-told identity." |
| 4.15 | 3 | §8 reasons | DISP:56–68 (`ok · warrant · blocked · thin-evidence · uncalibrated · dispersed · budget-exhausted · capacity-full · margin`) | The blueprint extends the set and renames three, so the shipped falsifier scripts do not port. | strings only | "Keep OSV's strings where they overlap; `budget-human` → `budget-exhausted`, `capacity` → `capacity-full`, `irreversible` aliases `warrant`; the new reasons are appended." |
| 4.16 | 2 | §9 gate order | CORE:254–266 | see 1.12 | the ordering | see 1.12 |
| 4.17 | 3 | §9 step 8 look value | DISP:183–186 | see 2.30 | with the added term | see 2.30 |
| 4.18 | 3 | §5 digest, revision, conservation | ING:219–231 (order-independent XOR digest) · ING:313–319 (equal revision a no-op, lower refused) · ING:247–250 (`conserves()` as an identity) | Adopted, and the blueprint corrects two shipped defects (the 32-bit revision ING:284; no back-fill IRRED:79). | already | "F-CONSERVE is written as ING's identity: `rows_in == ingested + replayed + truncated + refused_stale + heard + forming_skipped + echo_skipped`." |
| 4.19 | 3 | §3 ticks, §21 F-TICK | KC:248 (F5: lazy tick per gap, a resume tick from the persisted wall time; the kill: first-frame margin vs the lazily-ticked control within p95 noise) | Adopted verbatim in substance. | already | none |
| 4.20 | 3 | §4 cursor, mark, ring; §12 head-from-tape, torn row | KC:240 (F1), KC:104–105, KC:250 (F6) | Adopted verbatim in substance. | already | none |
